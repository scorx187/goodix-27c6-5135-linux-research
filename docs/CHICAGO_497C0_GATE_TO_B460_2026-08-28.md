# Chicago `0x1800497c0` gate to `0x18004b460` — 2026-08-28

Target: `AlgoChicago.dll`, tested Goodix `27c6:5135`, selector/family `0x0c`.

Static reverse-engineering checkpoint only. No proprietary binary, fingerprint frame/template, PSK/OTP, calibration payload, or unit-specific private data is committed.

## Proven caller flow

Inside the tested selector-`0x0c` path of `0x180049ba0`, after the already-proven `0x18004d6f0` base Q13 update and its separable median filter child `0x18004c3b0`, the caller executes:

```text
call 0x1800497c0
test eax,eax
je   0x18004a2de
```

The recovered call setup is:

```text
RCX = r12
RDX = geometry/config value from [rsp+0x50]
R8D = geometry/config value from [rsp+0x54]
R9D = DWORD from object at [rsp+0xb0]
stack args = additional config/type fields
```

Crucially, `scratch_A` (`r15`) is **not passed directly** to `0x1800497c0`.

Therefore, from this caller alone, `0x1800497c0` is proven to act as a boolean/control-flow gate for the following branch. There is no caller-level evidence that it directly rewrites `scratch_A`.

## Nonzero return path

If `EAX != 0`, the caller allocates a temporary `width*height*4` buffer and calls:

```text
0x18004b460
```

with:

```text
RCX = r12
RDX = pointer from [rsp+0xf0]
R8  = r15 = scratch_A
R9  = object/config pointer from [rsp+0xb0]
stack args include:
  geometry values
  rbp-side/static surface pointer
  pointer from [rsp+0x68]
  newly allocated temporary buffer
```

Then it allocates another `pixel_count*4` temporary and calls `0x18004b460` a second time. The second call again has:

```text
R8 = r15 = scratch_A
```

but different auxiliary stack/config pointers, including `rbx`, `[rsp+0x60]`, and the second temporary allocation.

After the second call, the second temporary is freed and execution rejoins at `0x18004a2de`.

## Consequence

The next decisive helper for the tested correction-surface data flow is:

```text
AlgoChicago.dll 0x18004b460
```

Reason: unlike `0x1800497c0`, `0x18004b460` is directly given the primary Q13 correction surface in `R8`, and it is invoked twice on the nonzero-gated branch.

Do not reverse `0x1800497c0` yet unless a later dependency requires the exact predicate that enables this branch.

## Immediate next task

Reverse exactly the PE runtime-function containing `0x18004b460` and prove:

1. exact function/logical boundary;
2. whether `R8 = scratch_A` is read-only or in/out;
3. whether either of the two calls writes `scratch_A` directly;
4. what the `width*height*4` temporary represents;
5. exact per-pixel/spatial/fixed-point relation if `scratch_A` changes;
6. whether the two call sites perform distinct passes over the same Q13 surface.

After that, if `0x18004b460` does not change `scratch_A`, skip the entire gated branch for denominator reconstruction. If it does, integrate its formula before the already-proven late Q13 factors.
